% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{compare_data}
\alias{compare_data}
\title{Compare two data frames}
\usage{
compare_data(
  x,
  y,
  by = NULL,
  context_rows = c(3L, 3L),
  context_cols = everything(),
  max_differences = Inf,
  tolerance = .Machine$double.eps^0.5
)
}
\arguments{
\item{x, y}{Data frames to compare.}

\item{by}{Optional character vector of key columns to match rows on, like
a join. Key columns must exist in both data frames and uniquely identify
rows in each. When \code{NULL} (the default), rows are matched by position
(row number). With \code{by}, the output is ordered by the key columns and
key columns are always included in the output.}

\item{context_rows}{Integer vector of length two indicating the number of context
rows to include before and after a difference row.}

\item{context_cols}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Columns to include as context.}

\item{max_differences}{Maximum number of differing rows to report. When
exceeded, only the first \code{max_differences} differing rows are returned
(with a message).}

\item{tolerance}{Numeric tolerance for comparing numeric values.}
}
\value{
A \link{datadiff_result} object: a list with elements \verb{$kind}
(\code{"identical"}, \code{"schema"}, or \code{"value"}), \verb{$columns}, \verb{$rows}, \verb{$by},
and \verb{$tolerance}. For \code{"schema"} (the frames have different column names
or types) \verb{$columns} holds a \code{\link[=compare_columns]{compare_columns()}} tibble and \verb{$rows} is
\code{NULL}; otherwise \verb{$rows} holds a tibble of the differing rows plus
context (empty when identical) and \verb{$columns} is \code{NULL}. Use
\code{\link[=get_differences]{get_differences()}} and friends to extract the differences.
}
\description{
Compare two data frames
}
\details{
Rows are matched by position (row number), or by key columns when \code{by}
is given. \code{x} and \code{y} must share at least one column, and shared columns
must have compatible types; otherwise a \code{"schema"} result is returned
instead of a row-level comparison. Rows present in only one data frame
are always reported as differences.
}
\examples{
x <- data.frame(id = 1:4, score = c(10, 20, 30, 40))
y <- data.frame(id = 1:4, score = c(10, 25, 30, 45))

# Rows are matched by position by default
compare_data(x, y, context_rows = c(1L, 1L))

# Match on a key column instead of position
compare_data(x, y, by = "id")

# A numeric tolerance treats near-equal values as equal
compare_data(x, y, tolerance = 10)
}
