% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{get_differences}
\alias{get_differences}
\alias{get_cell_differences}
\title{Extract the differences from a comparison result}
\usage{
get_differences(x, columns = NULL, ...)

get_cell_differences(x, columns = NULL, ...)
}
\arguments{
\item{x}{A \link{datadiff_result} from \code{\link[=compare_data]{compare_data()}} or \code{\link[=diffdata]{diffdata()}}.}

\item{columns}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Optional
selection of compared columns. \code{get_differences()} keeps rows that
differ in at least one selected column (rows present in only one frame
differ in every column, so they are always kept);
\code{get_cell_differences()} keeps cells from the selected columns.}

\item{...}{Passed on to methods.}
}
\value{
A tibble. \code{get_differences()}: \code{.row} (the row number the
difference came from), \code{.source} (\code{"x"} or \code{"y"}), and the data
columns. Rows present in only one frame appear in \code{get_differences()}
even when there are no non-key columns to report as cells.
\code{get_cell_differences()}: \code{.row}, the key columns (when the
comparison used \verb{by =}), \code{.column}, \code{.value_x}, and \code{.value_y}; values
are rendered as character (\code{NA} when the value is \code{NA} or the row
exists in only one frame — use \code{get_differences()} when native types or
that distinction matter). Both error on a \code{"schema"} result and return a
zero-row tibble for an \code{"identical"} one. A comparison truncated by
\code{max_differences} yields only the reported rows; compare with
\code{max_differences = Inf} to extract everything.
}
\description{
\code{get_differences()} returns the differing rows (context rows excluded) in
the stacked layout the package prints: each changed row appears twice,
the \code{x} version above the \code{y} version, with native column types
preserved. \code{get_cell_differences()} returns one row per differing cell,
with the two values side by side as character.
}
\examples{
x <- data.frame(id = 1:4, score = c(10, 20, 30, 40))
y <- data.frame(id = 1:4, score = c(10, 25, 30, 45))

result <- compare_data(x, y, by = "id")
get_differences(result)
get_cell_differences(result)
get_differences(result, columns = score)
}
