% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diffdata.R
\name{diffdata}
\alias{diffdata}
\title{Diff Data Frames}
\usage{
diffdata(
  x,
  y,
  by = NULL,
  context_rows = c(3L, 3L),
  context_cols = everything(),
  max_differences = 10,
  tolerance = .Machine$double.eps^0.5,
  output_file = NULL
)
}
\arguments{
\item{x, y}{Data frames to diff.}

\item{by}{Optional character vector of key columns to match rows on, like
a join. When \code{NULL} (the default), rows are matched by position. See
\code{\link[=compare_data]{compare_data()}} for details.}

\item{context_rows}{Integer vector of length two indicating the number of context
rows to include before and after a difference row.}

\item{context_cols}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Columns to include as context.}

\item{max_differences}{Maximum number of differing rows to report. Defaults
to 10 (unlike \code{\link[=compare_data]{compare_data()}}, which reports everything) to keep
reports fast to render.}

\item{tolerance}{Numeric tolerance for comparing numeric values.}

\item{output_file}{Optional file path to save the HTML report. If provided,
the report is saved to this location instead of opening in the viewer.}
}
\value{
Invisibly, a \code{datadiff_result} object (see \code{\link[=compare_data]{compare_data()}}). Called
for its side effect: when columns match and values differ it renders an
HTML report; when columns differ it prints the schema differences to the
console; when the frames are identical it reports no differences.
}
\description{
High-level function that compares two data frames and renders the result as
an HTML report in the RStudio viewer or browser.
}
\examples{
\dontshow{if (interactive()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
x <- data.frame(id = 1:5, score = c(10, 20, 30, 40, 50))
y <- data.frame(id = 1:5, score = c(10, 25, 30, 40, 55))

# Opens a styled HTML diff report in the RStudio viewer or browser
diffdata(x, y)

# Or write the report to a file instead of opening it
diffdata(x, y, output_file = tempfile(fileext = ".html"))
\dontshow{\}) # examplesIf}
}
