% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diff-class.R
\name{datadiff_result}
\alias{datadiff_result}
\title{The result of a data frame comparison}
\description{
\code{\link[=compare_data]{compare_data()}} and \code{\link[=diffdata]{diffdata()}} return a \code{datadiff_result}: a list
carrying one comparison outcome and the settings that produced it, with
\code{print()} and \code{summary()} methods.
}
\details{
A \code{datadiff_result} has five elements:
\itemize{
\item \code{kind} — \code{"identical"} (no differences), \code{"schema"} (the frames'
column names or types differ, so values were not compared), or
\code{"value"} (row-level differences were found).
\item \code{columns} — for \code{"schema"} results, the \code{\link[=compare_columns]{compare_columns()}} tibble
describing the column differences; otherwise \code{NULL}.
\item \code{rows} — for \code{"value"} and \code{"identical"} results, a tibble of the
differing rows plus surrounding context rows; otherwise \code{NULL}.
\item \code{by} — the key columns rows were matched on, or \code{NULL} for positional
matching.
\item \code{tolerance} — the numeric tolerance the comparison used.
}

For programmatic access prefer the accessors — \code{\link[=has_differences]{has_differences()}},
\code{\link[=n_differences]{n_differences()}}, \code{\link[=get_differences]{get_differences()}}, and \code{\link[=get_cell_differences]{get_cell_differences()}} —
over indexing into \code{rows}: the bookkeeping columns inside \code{rows}
(\code{.row}, \code{.join_type}, \code{.diff_type}, \code{.source}) and its attributes are
internal and may change.
}
\examples{
x <- data.frame(id = 1:4, score = c(10, 20, 30, 40))
y <- data.frame(id = 1:4, score = c(10, 25, 30, 45))

result <- compare_data(x, y)
result$kind
summary(result)
}
