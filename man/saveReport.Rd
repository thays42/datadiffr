% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rcompare-output.R
\name{saveReport}
\alias{saveReport}
\title{Save a comparison report}
\usage{
saveReport(
  compareObject,
  reportName,
  reportLocation = ".",
  HTMLReport = TRUE,
  showInViewer = TRUE,
  stylesheet = NA,
  printAll = FALSE,
  ...
)
}
\arguments{
\item{compareObject}{A comparison object returned by \code{\link[=rCompare]{rCompare()}}.}

\item{reportName}{File name for the report, without extension.}

\item{reportLocation}{Existing directory to write the report to.}

\item{HTMLReport}{If \code{TRUE}, render the report to HTML (requires
pandoc); the intermediate markdown is kept alongside it.}

\item{showInViewer}{If \code{TRUE} and the session is interactive, open the
rendered HTML report in the RStudio viewer or browser.}

\item{stylesheet}{Optional path to a CSS file for the HTML report, or
\code{NA} for the default style.}

\item{printAll}{If \code{TRUE}, the per-column detail tables include every
mismatching row instead of the first five.}

\item{...}{Ignored, for compatibility.}
}
\value{
\code{NULL}, invisibly.
}
\description{
Writes the \code{\link[=summary.datadiff_compare]{summary.datadiff_compare()}} report of a comparison to disk
as R Markdown and (optionally) rendered HTML, as in dataCompareR's
\code{saveReport()}.
}
\examples{
\dontshow{if (rmarkdown::pandoc_available()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
a <- data.frame(id = 1:3, value = c(1, 2, 3))
b <- data.frame(id = 1:3, value = c(1, 2.5, 3))
cmp <- rCompare(a, b, keys = "id")
saveReport(
  cmp,
  reportName = "example",
  reportLocation = tempdir(),
  showInViewer = FALSE
)
\dontshow{\}) # examplesIf}
}
