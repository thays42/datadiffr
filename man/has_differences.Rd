% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{has_differences}
\alias{has_differences}
\alias{n_differences}
\title{Test for and count differences in a comparison result}
\usage{
has_differences(x, ...)

n_differences(x, ...)
}
\arguments{
\item{x}{A \link{datadiff_result} from \code{\link[=compare_data]{compare_data()}} or \code{\link[=diffdata]{diffdata()}}.}

\item{...}{Passed on to methods.}
}
\value{
\code{has_differences()} returns \code{TRUE} or \code{FALSE}. \code{n_differences()}
returns the number of differing rows as an integer: \code{0L} when the
frames are identical, and \code{NA_integer_} for a \code{"schema"} result (values
were never compared). Truncation via \code{max_differences} does not affect
the count; every differing row found is counted.
}
\description{
\code{has_differences()} reports whether a comparison found any differences,
in values or in the frames' columns. \code{n_differences()} counts the
differing rows a value comparison found.
}
\examples{
x <- data.frame(id = 1:4, score = c(10, 20, 30, 40))
y <- data.frame(id = 1:4, score = c(10, 25, 30, 45))

result <- compare_data(x, y)
has_differences(result)
n_differences(result)
}
\seealso{
\code{\link[=get_differences]{get_differences()}} to extract the differences themselves.
}
