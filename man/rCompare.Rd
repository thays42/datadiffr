% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rcompare.R
\name{rCompare}
\alias{rCompare}
\title{Compare two data frames, dataCompareR style}
\usage{
rCompare(
  dfA,
  dfB,
  keys = NA,
  roundDigits = NA,
  mismatches = NA,
  trimChars = FALSE,
  tolerance = 0
)
}
\arguments{
\item{dfA, dfB}{Data frames to compare.}

\item{keys}{Character vector of key columns used to match rows, or \code{NA}
(the default) to match rows by position. Keys must identify rows
uniquely in each frame.}

\item{roundDigits}{If not \code{NA}, round double columns to this many digits
before comparing.}

\item{mismatches}{If not \code{NA}, the maximum total number of mismatching
values allowed; an error is thrown when the comparison finds more.}

\item{trimChars}{If \code{TRUE}, trim leading and trailing whitespace from
character columns before comparing.}

\item{tolerance}{Numeric tolerance for comparing numeric values, as in
\code{\link[=compare_data]{compare_data()}}. Defaults to \code{0} (exact comparison, as dataCompareR
behaves).}
}
\value{
An object of class
\code{c("datadiff_compare", "dataCompareRobject")}: a list with elements
\code{meta}, \code{colMatching}, \code{rowMatching}, \code{cleaninginfo}, \code{mismatches},
and \code{matches}, mirroring dataCompareR's return value.
}
\description{
A drop-in replacement for \code{rCompare()} from the archived 'dataCompareR'
package. Compares two data frames — by row position, or by key columns
when \code{keys} is supplied — and returns a summary object with the same
shape as a \code{dataCompareRobject}, so existing code and scripts written
against 'dataCompareR' keep working.
}
\details{
Following the dataCompareR contract: column names are cleaned with
\code{\link[=make.names]{make.names()}} and matched case-insensitively (shared columns are
reported in upper case, sorted alphabetically); factors are compared as
character and the coercion is recorded in \code{cleaninginfo}; \code{NA} matches
\code{NA} and \code{NaN} matches \code{NaN}, but \code{NA} does not match \code{NaN}; columns
whose classes differ between the two frames are reported as mismatching
on every row. Without \code{keys}, the longer frame is truncated to the
length of the shorter and rows are compared by position.

\code{tolerance} is a datadiffr extension (dataCompareR only offers
\code{roundDigits}). The default \code{0} keeps faithful exact comparison.
}
\examples{
a <- data.frame(id = 1:3, value = c(1, 2, 3))
b <- data.frame(id = 1:3, value = c(1, 2.5, 3))
rCompare(a, b, keys = "id")
}
