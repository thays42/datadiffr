.Rproj.user
.Rhistory
.RData
.Ruserdata
/doc/
/Meta/

# Obsidian vault link
.obsidian-notes

# Local coverage output
coverage.xml

# Vignette build artifacts
inst/doc
vignettes/*.html
vignettes/*.R
.positai

# pkgdown site build output — built and deployed by CI (pkgdown.yaml);
# local builds via `make site` are previews only, never commit them
/docs/
*.pkgdown-bak
